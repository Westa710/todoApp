# JDBC minimal sample

PostgreSQL に JDBC で接続する最小サンプルです。

## Run

```bash
cd backend
gradle run
```

## Environment variables

- `DB_HOST` - default: `db`
- `DB_PORT` - default: `5432`
- `DB_NAME` - default: `postgres`
- `DB_USER` - default: `postgres`
- `DB_PASSWORD` - default: `postgres`
